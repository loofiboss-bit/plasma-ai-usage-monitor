# v10.0.1 Accuracy Patch Release Checklist

## Scope
- Fix the installed `ProviderCard.qml` catalog singleton import so v10 source badges resolve `ProviderPricingCatalog`.
- Keep the v10.0.0 Accuracy feature scope unchanged.

## Validation
- [ ] Run `just check`.
- [ ] Run `just test`.
- [ ] Run `just release-check`.
- [ ] Build and install the RPM locally.
- [ ] Restart `plasmashell` and confirm the journal no longer reports `ProviderPricingCatalog is not defined`.

## Publication
- [ ] Publish GitHub release `v10.0.1`.
- [ ] Verify COPR builds `10.0.1-1`.
- [ ] Update KDE Store product `2353976` with the fixed v10.0.1 plasmoid artifact.
