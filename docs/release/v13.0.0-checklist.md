# v13.0.0 Provider Intelligence release checklist

## Code and data

- [x] No scheduled inference endpoint in default provider paths.
- [x] Metric Contract v2 preserves null, source, quality, scope, window, currency, and reset metadata.
- [x] SQLite v3-to-v4 migration is transactional, idempotent, backed up, and retains compatibility history.
- [x] OpenRouter uses `/api/v1/key`; Gemini uses read-only dynamic model discovery.
- [x] Catalog v5 and ProviderManager describe existing and launch providers.
- [x] Fireworks billing remains disabled by default and its read-only discovery contract is covered by deterministic fixtures.

Provider API keys and live provider accounts are optional release evidence and never block publication. Release validation must be reproducible without KWallet entries, vendor accounts, or billable requests.

## Validation

- [ ] Full Debug/ASan/UBSan, CTest, QML lint/import, payload, reproducibility, SBOM, and Fedora 44 gates green on the RC commit.
- [x] Credential-free OpenRouter/LiteLLM/Cerebras/Fireworks/Perplexity contracts pass against deterministic fixtures.
- [x] Fedora 44 clean install, v12.0.3 upgrade, rollback, and removal verified.
- [ ] GitHub, COPR, KDE Store, screenshots, checksums, SBOM, and release notes point to the exact RC commit.

## Promotion

- [ ] Publish `v13.0.0-alpha.1`, `beta.1`, and `rc.1` in the planned order.
- [ ] Promote the same commit to `v13.0.0` after deterministic gates and prerelease readback; no timed soak is required.

## Prerelease operations evidence

- [x] `v13.0.0-alpha.1` published from commit `3b5c08ba2ed5a4c611076ee6b387ae083a5ec121`; GitHub checks, checksums, source SPDX SBOM, source version, and plasmoid metadata verified by readback.
- [x] Release policy refreshed 2026-07-17: missing or rejected provider credentials do not block beta, RC, or stable publication. Runtime users still configure their own keys for authenticated providers.
- [x] Perplexity public model discovery returned HTTP 200 with 33 models on 2026-07-14 without a credential, matching the capability-limited launch profile.
- [x] COPR build `10714754` was automatically triggered by the prerelease tag and canceled before publication; webhook rebuild is disabled so prerelease tags cannot masquerade as stable RPM updates.
- [x] Fedora 44 container lifecycle passed with the COPR `12.0.3-1.fc44` RPM and exact alpha `13.0.0-1.fc44` RPM: v12 install, v13 upgrade, v12 downgrade, v13 re-upgrade, removal, and user-data preservation. The gate is reproducible with `just rpm-lifecycle-check <v12-rpm> <v13-rpm>`.
- [x] A separate clean Fedora 44 container passed v13 install, payload verification, removal, and user-data preservation.
- [x] Deterministic demo contracts exercise OpenRouter `GET /key`, LiteLLM spend, Cerebras/Fireworks discovery, and Perplexity discovery; the retired OpenRouter `/auth/key` route is rejected.
- [x] Stable tag tooling rejects missing, lightweight, divergent, or non-main `rc.1` promotion lineages while allowing immediate promotion after deterministic gates pass.
- [ ] Publish to COPR only through an explicit stable promotion after `main`, the stable tag, and the exact RC commit resolve to the same SHA; keep automatic prerelease rebuilds disabled.
