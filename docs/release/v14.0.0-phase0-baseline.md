# v14.0.0 Phase 0 Baseline

Date: 2026-07-18
Branch: `main`
Commit: `0c31caaf1087914950a950c86667594a47356c9c`
Stable version: `13.0.0`

The implementation baseline was clean and matched `origin/main`. The earlier
planning baseline, `07a9a7ddd2be66e937a13d7d93433dd98062236c`, is its runtime-code
parent; the intervening commit added the v14 plan and desktop metadata only.

## Verified gates

| Gate | Baseline result |
| --- | --- |
| `just build-debug` | Passed |
| `ctest --preset debug --output-on-failure` | Passed, 29/29 tests |
| `just check` | Passed |
| `just qml-lint` | Passed under the repository's error-only policy; existing warnings remain visible |
| Strict native QML import | Passed; plugin version `13.0.0` |
| `python3 scripts/check_package_payload.py` | Passed |
| Offscreen `plasmawindowed` smoke | Passed for Overview, History, and Analyst |

The offscreen runtime command was:

```bash
QT_QPA_PLATFORM=offscreen dbus-run-session -- bash scripts/smoke_test_plasmoid.sh build/debug
```

## Phase 0 compatibility boundary

Phase 0 does not change `VERSION`, provider or subscription catalogs,
`package/contents/config/main.xml`, SQLite schema v4, the KWallet folder, or any
secret identifier. Existing v13 configuration, history, and KWallet entries
remain the upgrade authority.
