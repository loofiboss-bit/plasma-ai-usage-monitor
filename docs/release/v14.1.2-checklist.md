# v14.1.2 Daily Truth release-candidate checklist

This checklist stops at a locally verified candidate. Commit, tag, push, COPR,
KDE Store, GitHub release, and other remote publication require a separate
instruction.

Provider API keys and live provider accounts are optional release evidence and never block publication.

## Scope and lineage

- Baseline commit: `43ae7ff7537b052740c2b935449a6f49d2823821`
- Working branch: `fix/v14.1.2-daily-truth`
- Required upgrade and rollback base: exact package version `14.1.1`
- Candidate version: `14.1.2`
- Candidate commit: not created; local candidate only

## Behavior and contracts

- [x] Provider-only, tool-only, mixed, connectivity-only, needs-configuration,
  and no-source Daily Truth fixtures pass.
- [x] Available cost and remaining-request zeroes remain zero; unavailable
  values remain absent or display an em dash.
- [x] Actual current/day spend remains currency-separated and estimated cost
  does not enter actual compact modes.
- [x] Healthy, warning, critical, connectivity, and unverified badge states pass.
- [x] Missing heatmap days, explicit zero, and positive activity remain distinct.
- [x] Positive ratio, explicit zero output, zero input, and empty series pass.

## Preserved user data and identities

- [x] `package/contents/config/main.xml` is byte-identical to v14.1.1; no KConfig
  key or stored compact-mode value changed.
- [x] `plugin/secretsmanager.h` and `plugin/secretsmanager.cpp` are byte-identical
  to v14.1.1; KWallet folder and key identities are unchanged.
- [x] `PRAGMA user_version` remains `4` and the v12/v13 migration fixtures pass.
- [x] Upgrade, rollback, re-upgrade, reinstall, and removal preserve the SQLite
  history fixture, Plasma configuration fixture, and KWallet fixture.
- [x] Package identity, Plasma install path, QML URI, notifyrc installation, and
  AppStream installation remain unchanged.

## Local verification gates

- [x] Fresh debug configure and build
- [x] Targeted Daily Truth, Analyst, SQLite, config, and KWallet tests
- [x] Full debug CTest suite
- [x] Static repository checks and generated-wiki drift check
- [x] QML lint and strict native QML import
- [x] Full plasmoid smoke under isolated D-Bus and Xvfb
- [x] Shellcheck for release and demo scripts
- [x] Strict release check and Fedora 44 container check
- [x] ASan and UBSan suite
- [x] Source, plasmoid, SRPM, and binary RPM candidate artifacts
- [x] Fedora 44 RPM lifecycle using v14.1.1 base and v14.1.2 candidate
- [x] One isolated Breeze Dark media session captures all eight v14 assets and
  produces a manifest matching version 14.1.2.

## Evidence

| Item | Local result |
| --- | --- |
| Candidate tree | Uncommitted `fix/v14.1.2-daily-truth` from `43ae7ff7537b052740c2b935449a6f49d2823821`; `git diff --check` passes |
| Debug tests | Fresh configure/build passes; targeted 7/7 and full CTest 41/41 pass |
| Sanitizers | Fresh ASan/UBSan configure/build and CTest 41/41 pass |
| Config/KWallet/SQLite diff | KConfig and SecretsManager are byte-identical to the base; schema remains v4; migration and portability tests pass |
| Candidate artifacts | Source, plasmoid, SRPM, and binary RPM payload checks pass; final local hashes are recorded in the implementation handoff |
| Binary RPM | Clean Fedora 44 mock build from the final candidate source |
| RPM lifecycle | PASS against exact `14.1.1-1.fc44` base, including data preservation and removal behavior |
| Fedora 44 host audit | Release and clean-container gates pass. The host-only package audit still reports `protobuf-devel` and `protobuf-compiler` absent from the system RPM database; matching Fedora 44 RPMs were extracted into `/tmp` for fresh local builds without changing the host. |
| Media manifest | Eight assets, version 14.1.2, isolated session `745bc434-24c4-490e-8eaa-37c4132c545c` |

## Explicit publication stop

- [x] No commit or tag created
- [x] No branch pushed and no pull request opened
- [x] No GitHub release, COPR build, or KDE Store update performed

If publication is separately authorized after this checklist is complete:
Publish `v14.1.2` directly from the verified `main` commit.
